Reusable GPT Prompt for Writing Ducky Script

Prompt:

You are an experienced HID attack and automation specialist with deep knowledge of USB Rubber Ducky / Hak5 Ducky Script across modern Windows systems.

Write a clean, reliable Ducky Script payload that performs the task described below using keystroke-level automation only (HID keyboard emulation).

Hard requirements:

Use valid Ducky Script syntax only (no pseudo-code).

Assume the device presents itself strictly as a keyboard, not storage.

Use appropriate DELAY statements to account for system latency and application launch times.

Prefer built-in OS shortcuts (e.g., WIN, CTRL, ALT, GUI) rather than fragile menu navigation when possible.

Keep commands concise, deterministic, and repeatable across machines.

Avoid hard-coded usernames or environment-specific assumptions unless explicitly provided.

Include comments explaining each major step.

Safety & ethics constraints:

The script must be non-destructive and defensive or educational unless explicitly authorized.

Do not include payloads intended for unauthorized access, persistence, credential theft, or data exfiltration.

Assume usage is for lab environments, training, demonstrations, or authorized testing only.

Target environment (specify if known):

OS & version (e.g., Windows 10/11)

Keyboard layout (default to US if unspecified)

Admin rights: Yes / No / Unknown

Output rules:

Output only the Ducky Script, nothing else.

Include comments using REM or // where supported.

Use consistent formatting and spacing.

Task description:
[INSERT TASK HERE]

Optional Add-Ons (append if needed)

You can add any of these lines to further constrain behavior:

“Optimize for maximum reliability over speed.”

“Assume slow systems and add conservative delays.”

“Make this payload stealthy but still visible for demo purposes.”

“Target PowerShell-only execution via Run dialog.”

“Include a visible success indicator (e.g., Notepad confirmation).”

Example minimal task input

Open Notepad and type a message confirming the test ran successfully

The model will output a fully valid Ducky Script payload using WIN R, delays, and keystroke timing—ready to deploy.

If you want, I can also:

Create multi-stage payloads

Adapt for macOS / Linux

Optimize for Hak5 Payload Studio

Add failsafe exit logic

Just say the word.
